import React from 'react';
import PageHeader from '../components/pageHeader';

const Home = () => (
  <div className="container">
    <PageHeader title="Hello" />
    <p>Project requirements didn't indicate much for here - maybe try a different page.</p>
  </div>
);

export default Home;